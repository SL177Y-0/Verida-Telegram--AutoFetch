import { VeridaContract } from "@verida/web3";
import { VdaClientConfig, BlockchainAnchor, TContractNames } from '@verida/types';
import { ethers } from "ethers";
export declare class VeridaClientBase {
    protected config: VdaClientConfig;
    protected blockchainAnchor: BlockchainAnchor;
    protected didAddress?: string;
    protected vdaWeb3Client?: VeridaContract;
    protected readOnly: boolean;
    protected contract?: ethers.Contract;
    constructor(config: VdaClientConfig, contractName: TContractNames);
}
