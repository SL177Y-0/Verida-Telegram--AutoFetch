import { AuthContext, AuthTypeConfig, SecureContextPublicKey } from "@verida/types";
import { ServiceEndpoint } from "did-resolver";
import Account from "./account";
export declare class AuthType {
    protected contextAuth?: AuthContext;
    protected account: Account;
    protected contextName: string;
    protected serviceEndpoint: ServiceEndpoint;
    protected signKey: SecureContextPublicKey;
    constructor(account: Account, contextName: string, serviceEndpoint: ServiceEndpoint, signKey: SecureContextPublicKey);
    getAuthContext(config: AuthTypeConfig): Promise<AuthContext>;
    setAuthContext(contextAuth: AuthContext): void;
    disconnectDevice(deviceId?: string): Promise<boolean>;
}
