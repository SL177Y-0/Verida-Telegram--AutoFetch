"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ethers_1 = require("ethers");
var Wallet = /** @class */ (function () {
    function Wallet(privateKey, network) {
        this._network = network;
        this.load(privateKey);
    }
    Wallet.createRandom = function (network) {
        if (network === void 0) { network = 'polpos'; }
        var wallet = ethers_1.Wallet.createRandom();
        return new Wallet(wallet.privateKey, network);
    };
    Wallet.prototype.load = function (privateKey) {
        var wallet;
        if (privateKey.substr(0, 2) == "0x") {
            wallet = new ethers_1.Wallet(privateKey);
        }
        else {
            wallet = ethers_1.Wallet.fromMnemonic(privateKey);
        }
        this._did = "did:vda:".concat(this._network, ":").concat(wallet.address);
        this._privateKey = wallet.privateKey;
        this._publicKey = wallet.publicKey;
    };
    Object.defineProperty(Wallet.prototype, "did", {
        get: function () {
            return this._did;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Wallet.prototype, "privateKey", {
        get: function () {
            return this._privateKey;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Wallet.prototype, "publicKey", {
        get: function () {
            return this._publicKey;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Wallet.prototype, "privateKeyBuffer", {
        get: function () {
            return Buffer.from(this._privateKey.substr(2), 'hex');
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Wallet.prototype, "publicKeyBuffer", {
        get: function () {
            return Buffer.from(this._publicKey.substr(2), 'hex');
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Wallet.prototype, "privateKeyBase58", {
        get: function () {
            return ethers_1.utils.base58.encode(this._privateKey);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(Wallet.prototype, "publicKeyBase58", {
        get: function () {
            return ethers_1.utils.base58.encode(this._publicKey);
        },
        enumerable: false,
        configurable: true
    });
    return Wallet;
}());
exports.default = Wallet;
//# sourceMappingURL=wallet.js.map